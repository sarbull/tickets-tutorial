import { TicketsContainer } from './tickets.container';
import { TicketsModule } from './tickets.module';
import { reducer } from './tickets.reducers';
import { TicketState, TicketInterface, Ticket } from './tickets.model';

export {
  TicketsContainer,
  TicketsModule,
  reducer,
  TicketState,
  TicketInterface,
  Ticket
}
